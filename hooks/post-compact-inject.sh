#!/bin/bash
# Re-inject critical rules after context compaction
cat << 'RULES'
CRITICAL REMINDERS (re-injected after compaction):
1. Read today's daily journal (memory/YYYY-MM-DD.md) to restore context
2. MEMORY.md contains your curated long-term memory — reference it
3. Use QMD search tools for information beyond current context
4. Write important new information to MEMORY.md proactively
5. Append session updates to today's daily journal periodically
RULES
